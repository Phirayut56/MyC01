#include <stdio.h>

int main(){
    
    int x, y, z;
    y = 3;
    z = 4;

    if ((y > x) && (x > y)) {
        z = x % 5 + y * 3;
        printf("Value is %d\n", z);
    }
    printf("x % z\n");
    printf("Sawasdee");

    return 0;
}